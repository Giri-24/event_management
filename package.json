{
  "name": "event-management-root",
  "version": "1.0.0",
  "description": "Root package.json to route Render deployments perfectly into the backend folder",
  "scripts": {
    "install": "cd backend && npm install",
    "build": "cd backend && npm install",
    "start": "cd backend && node server.js"
  }
}
